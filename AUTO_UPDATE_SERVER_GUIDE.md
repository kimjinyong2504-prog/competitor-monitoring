# HTML 자동 업데이트 가이드

## 개요

HTML 파일을 열면 자동으로 최신 정보로 보고서가 업데이트되도록 하는 기능입니다.

## 작동 방식

1. **로컬 웹 서버 실행**: `local_server.py`를 실행하면 웹 서버가 시작됩니다.
2. **브라우저에서 접속**: 브라우저에서 `http://localhost:5000`으로 접속합니다.
3. **자동 감지**: 페이지가 1분마다 데이터 변경을 확인합니다.
4. **자동 새로고침**: 새로운 데이터가 있으면 자동으로 페이지를 새로고침합니다.
5. **수동 업데이트**: 우측 하단의 "🔄 업데이트" 버튼을 클릭하여 수동으로 업데이트할 수 있습니다.

## 사용 방법

### 1. 필요한 패키지 설치

```bash
pip install flask flask-cors
```

또는

```bash
pip install -r requirements.txt
```

### 2. 로컬 서버 실행

```bash
python local_server.py
```

서버가 시작되면 다음 메시지가 표시됩니다:
```
서버 시작: http://127.0.0.1:5000
보고서 보기: http://127.0.0.1:5000/
```

### 3. 브라우저에서 접속

브라우저에서 `http://localhost:5000` 또는 `http://127.0.0.1:5000`으로 접속합니다.

### 4. 자동 업데이트 확인

- 페이지가 열리면 우측 하단에 "🔄 업데이트" 버튼이 표시됩니다.
- 1분마다 자동으로 데이터 변경을 확인합니다.
- 새로운 데이터가 있으면 자동으로 페이지가 새로고침됩니다.

## 고급 옵션

### 포트 변경

```bash
python local_server.py --port 8080
```

### 백그라운드 자동 업데이트 활성화

서버가 1시간마다 자동으로 데이터를 업데이트합니다:

```bash
python local_server.py --auto-update
```

### 다른 호스트에서 접속 허용

```bash
python local_server.py --host 0.0.0.0
```

이렇게 하면 같은 네트워크의 다른 기기에서도 접속할 수 있습니다.

## API 엔드포인트

### GET `/api/status`
데이터 상태 확인

**응답 예시:**
```json
{
  "data_exists": true,
  "report_exists": true,
  "last_updated": "2025-12-08 10:30:00",
  "data_size": 123456,
  "report_size": 234567,
  "report_modified": "2025-12-08 10:30:00"
}
```

### POST `/api/update`
보고서 수동 업데이트

**응답 예시:**
```json
{
  "success": true,
  "message": "업데이트 완료",
  "output": "...",
  "error": ""
}
```

### GET `/api/data`
JSON 데이터 파일 반환

## 문제 해결

### 서버가 시작되지 않을 때
- 포트가 이미 사용 중일 수 있습니다. 다른 포트를 사용하세요: `--port 8080`
- Flask가 설치되어 있는지 확인하세요: `pip install flask flask-cors`

### 자동 업데이트가 작동하지 않을 때
- 브라우저 콘솔을 확인하세요 (F12)
- 로컬 서버가 실행 중인지 확인하세요
- 파일을 직접 열면 (file://) 자동 업데이트가 작동하지 않습니다. 반드시 웹 서버를 통해 접속해야 합니다.

### 업데이트 버튼이 보이지 않을 때
- 브라우저 콘솔에서 오류를 확인하세요
- 로컬 서버를 통해 접속했는지 확인하세요 (http://localhost:5000)

## 보안 참고사항

- 이 서버는 로컬 개발용입니다. 인터넷에 공개하지 마세요.
- `--host 0.0.0.0`을 사용하면 같은 네트워크의 모든 기기에서 접속할 수 있습니다.
- 프로덕션 환경에서는 적절한 인증 및 보안 조치를 추가하세요.

